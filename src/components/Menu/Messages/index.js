import MenuLayout from 'Layout/MenuLayout';
import PropTypes from 'prop-types';
import React from 'react';

export const Messages = () => {
  return <MenuLayout>Hello World</MenuLayout>;
};

Messages.propTypes = {
  props: PropTypes,
};

export default Messages;
